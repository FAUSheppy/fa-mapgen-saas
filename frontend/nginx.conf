server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    location /api-dev/ {
        proxy_pass "http://localhost:5000/";
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
